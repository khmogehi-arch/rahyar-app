PORT=4000
JWT_SECRET=change-this-secret-in-production
VISITOR_APP_URL=http://localhost:5174
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-password

# Only needed when deploying to a serverless platform (e.g. Vercel), whose
# filesystem is read-only except for /tmp. Leave unset for local dev.
# DB_PATH=/tmp/data.sqlite3
# UPLOADS_DIR=/tmp/uploads
